@namespace Nekai.Razor
@inherits NekaiComponentBase

<div data-toggle="tooltip" title="@Text">
    @ChildContent
</div>

@code {
    [Parameter]
    public RenderFragment? ChildContent { get; set; }
    [Parameter, EditorRequired]
    public string Text { get; set; } = "";
}